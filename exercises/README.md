// This go.mod fences the exercise files off from the root module: each
// folder holds a starter, a solution and a hidden test for ONE file of a
// part, so they only compile when overlaid on that part (see validate.py
// and server/main.go). Nothing in here is meant to build on its own.
